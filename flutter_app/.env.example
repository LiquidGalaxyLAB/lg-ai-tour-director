# Gemini API Key for location discovery and narration writing
GEMINI_KEY=

# Google Maps API Key for Geocoding and static preview maps
MAPS_KEY=

# Liquid Galaxy Connection Defaults
LG_HOST=
LG_SSH_PORT=22
LG_USERNAME=lg
LG_PASSWORD=
LG_SCREENS=3

# Unsplash Access Key (image fallback when Wikipedia has no photo)
# Free key: unsplash.com/developers -> New Application (50 req/hour)
# Only the Access Key is needed (Secret Key is for OAuth, unused here).
UNSPLASH_ACCESS_KEY=
